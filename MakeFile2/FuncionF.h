#ifndef FUNCION_H_ //nombre del archivo nos indica que pasara si no esta definida la funcion, si la funcion ya esta definia no se vuelve a definir
#define FUNCION_H_

extern int saludo; //extern es para que no declare la variable varias veces en distintos archivos
void hola();

#endif //en los .h solo se declaran las funciones o las variables